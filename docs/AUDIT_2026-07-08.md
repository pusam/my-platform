# AUDIT 2026-07-08 — 불변식 기준 전수 감사 리포트 (2회차)

> 감사 세션(코드 수정 금지 — 발견은 리포트로만). 근거는 전부 파일:라인. 방법론 = AUDIT_2026-07-07(E) 7축 승계.
> 표기: **P0**=즉시 위험(실손실/보안) · **P1**=실버그(오동작·완화 존재) · **P2**=정합성/저위험 · **P3**=주석/문서/미세.
> 대상 = 전회차(2026-07-07(E)) 이후 추가분 전부: V43~V45(수동 저널·SellInflight) · KIS 토큰(expires_in·401) ·
> 표시 3작업(신호이력/재료·연속순매수/RVOL/진입위치/보드↔상세) · 악재 조기경보 · 기관키 오타·이중인코딩·공매도 null · NXT 4Phase(`36ce8d8..6d9c740`).
> 감사 방식 = 5개 독립 감사관 병렬(코드 클러스터별, 축 교차) + 직접 재검증. **NXT 코드는 저자(본인) 아닌 독립 감사관이 검증**(객관성).

## 🔒 안전 요약 — **P0/P1 없음.** 활성 주문 경로·봇 안전 게이트 무결.
활성 KIS 주문 경로(BUY/SELL·NXT 신규 경로 포함)에 게이트 구멍 0, flag-OFF 바이트 동일 증명, 저널 봇 격리 증명, 전회차 P1 3건 수정 정합 확인. **즉시 조치 필요 항목 없음.**

## 요약 (위험도순)

| # | 위험도 | 발견 | 위치 | 조치 |
|---|---|---|---|---|
| 1 | **P2** | 악재경보 dedup = check-then-act + `alert_history.alert_key` **UNIQUE 부재** → 동일 종목·일자 재료 동시 최초분류 시 텔레그램 **중복 발송**(금전 무관·저확률). "종목×일자 1회 멱등" 설계 주장이 DB영속이나 **비원자적** | `CatalystRiskAlertService.java:55-88,133-145` · `AlertHistory.java:12-15` | 리포트(수정 별도 세션 — §4d④ 조건부 UPDATE 패턴 이식 권장) |
| 2 | **P3** | 저널 평가 pct/alpha **scale 2** vs `signal_outcome` **scale 4** — 공용 `isHit` 쓰나 입력 정밀도 상이 → 경계(3.00%/α0)에서 두 화면 hit 판정 불일치. "동일 잣대" 주장과 모순 (전회차 #7 **미수정 이월**) | `ManualTradeJournalService.java:240,244` vs `SignalOutcomeService.java:219,225,287,296` | 리포트(산식 결과값 변경이라 별도 세션) |
| 3 | **P3** | `applyNxtRouting` javadoc 이 미확정-NXT 거부를 "RealTradeService 가 markFailed 처리"로 서술하나 markFailed 는 **BUY 전용**(NXT 는 SELL 경로라 rt_cd≠0 throw). **안전 동작은 정확**(killswitch 미발동), 주석 메커니즘명만 오기 | `KoreaInvestmentService.java:1335` | 리포트(주석만) |
| 4 | **P3** | `FlywayMigrationTest` 가 V43/V44/V45 객체 **타겟 단언 없음**(clean-apply 는 `pending().isEmpty()` 로 커버되나 V36/V40/V41/V42 처럼 존재 단언 미보유) | `FlywayMigrationTest.java:82-102` | 리포트(회귀 가드 1줄 추가 권장) |
| 5 | **P3** | 수동 매매 저널(V43/V44) — VERIFICATION_BACKLOG 티켓 부재(추적성). 재료 워밍 관심/보유 확장은 P2-CAT3 범위 초과인데 티켓 미갱신 | `ManualTradeJournalService` · `CatalystWarmingService.java:24-35` | 리포트(추적성) |
| 6 | **P3** | DART rceptNo dedup 도 non-atomic GET/PUT(단 4분 락 직렬화·시각 분리로 실위험 낮음) / Gemini `callGeminiApiPatient` 는 전역 rate 게이트 우회(주간 리포트 전용·기존코드) | `DartDisclosureMonitorService.java:210-216` · `GeminiService.java:449` | 리포트(저위험/기존) |
| 7 | **P3** | RVOL 실측 0.03x 가 `toFixed(1)`→"0.0x" 표시(진짜 값이라 §4c 위장 아님, 미세 오독 가능) | `QuickSummaryBar.vue:130` | 리포트(표시 정밀도) |

---

## 전회차 P1 3건 — 수정 정합 재검증 (직접 확인, ✅ 전부 정합)

| 전회차 | 수정 | 재검증 결과 |
|---|---|---|
| **P1-1/P1-2** 이중 인코딩(§16-10) | `20cb176` | ✅ `GoogleNewsService.buildSearchUrl:134`·`StockPriceService.buildNaverSearchUrl:747` 둘 다 **`URI` 반환**(`UriComponentsBuilder…encode(UTF_8).build().toUri()`) → RestTemplate 재인코딩 없음. §16-10 준수. (독립 감사관도 Naver 회귀 무·DART 신규는 ASCII-only 확인.) |
| **P1-3** 공매도 ZERO 위장(§4c) | `f0d539a` | ✅ `getShortSellingRatio` 3결측(데이터 전무 :524·미포함/orElse :530·예외 :533) 전부 **null**. `BuyChecklistService` null→`dataMissing(true)`+"미수집", **필수 게이트에서 dataMissing 제외**(:258)라 결측이 NOT_RECOMMENDED 강등 안 함. `isHighShortSellingStock`(:594-596) null→false=진입 통과(PriceSanityGuard 결측=통과 극성 일치). 캐시(:613)는 non-null 만 저장 → 위장 ZERO 캐시 불가. §4c 준수. |

---

## 축별 결과 (전수 확인 근거)

### ③ §4d fail-closed 호출그래프 — **구멍 0 (증명 완료, 최우선 축)**

**SELL in-flight 마커(V45) 커버리지 — OK(증명).** `kisService.sellStock` 의 `main` 트리 유일 호출부 = `RealTradeService.java:464`(3-arg REGULAR)·`:465`(4-arg session), 둘 다 단일 5-arg `sell` 바디 안. 4-arg `sell`(:386-388)→5-arg(REGULAR) 위임. 마커 `tryAcquire` 는 분기 **이전** `:445`, `release` 는 `finally :540-544`(killswitch/예외 경로 포함). 봇 5경로(2199·2658·2759/60·3124·3466)·수동(`PaperTradingController:360`) 전부 이 바디 경유 — 우회 sellStock 없음(전수 grep). `decideSellGate` 극성 = DB오류→`PROCEED_UNGUARDED`(fail-open, BUY 멱등키 fail-closed 의 의도적 반대, `BotSellInflightService:93-99`), `SKIP_CONCURRENT` 는 try 이전 throw(:450)라 남의 마커 미삭제.

**401 토큰 무효화 = 주문 재시도 없음 — OK(증명).** `placeOrder` catch(`KoreaInvestmentService:1432-1447`)에서 `invalidateTokenOnAuthFailure` 는 토큰 캐시 null 화만(:141-148), **재-POST 없음**(catch 는 거부바디/null 반환). `KisApiRateLimiter.execute(...,3)` 재시도 루프도 주문 재제출 불가 — `placeOrder` 가 rethrow 안 하므로 `apiCall.get()` 정상 반환, limiter 재시도 미진입(재시도는 세마포어 획득 타임아웃=주문 발사 전에만, `:89-94`). **KIS 비멱등 — 이중 제출 채널 0.**

**저널 봇 격리(record-only) — OK(증명).** `ManualTradeJournalService` 의 모든 `save()` = `journalRepository`(:113·128·199)뿐. 봇 테이블 접근 = `botPositionRepository.findAll`(:161) **read-only**(섹터 노출 경고용). RealTradeService/VirtualTradeService/주문경로 참조 0. 봇 테이블 write·주문 트리거 0.

### ① 단일 시세경로 — **하드 위반 0 (증명)**

신규 코드 전부 준수: 저널 RVOL 캡처 = `stockPriceService.getStockPricesFromCacheOnly`(cache-only 단일경로, KIS 신규호출 0), 저널 평가가격 = `getStockPrice`(canonical), RVOL 분자 = `resolveTradingValue`(단일 출처, 보드/단건/스냅샷 재사용), 벤치마크 `bmPrice` = KOSPI 지수(`getIndexPrice 0001` — 종목 단일경로 대상 아님, `signal_outcome.bm_price_at_signal` 과 동일). 보드 집계 N+1 없음(catalyst/price/trackRecord/RVOL/streak 전부 IN·GROUP BY 벌크). RVOL 분자/분모 소스 비대칭(장중 저평가)은 Javadoc·UI 명시(표시 전용, P3).

### ② §4c 결측 위장 — **신규 코드 전수 준수 (증명)**

- **RVOL**: `computeRvol` 20일 미만·todayValue≤0·avg≤0 전부 **null**(`RvolService:110-117`), 결측일 skip(:98). 프론트 null→미표시(`QuickSummaryBar:54,130` v-if 가드, `SectionJudgmentBoard:90`). **"장전 0.0x 위장" 부재**(장전=null=미표시). 실측 sub-0.05x 만 "0.0x"(진짜 값, P3 미세).
- **저널 12필드**: 전부 `quiet()` null-on-fail — 카테고리점수(snap null 시 미설정)·catalyst(NONE→null·§4b read-only)·regime(client 미가용 null)·rsi/atr/5일수익(결측 null)·bmPrice(:212-217 null). 통계 zero-sample→null·n<10→insufficientSample.
- **재료**: Naver 다운/Gemini circuit/파싱실패 전부 **미캐시**(재시도), 소스 가용+뉴스0 만 NONE 캐시(`StockCatalystService:76-96,147,170,183`). classifyBatch 후에도 하드닝 intact.
- **신호이력**: pending(evaluatedAt null)≠miss 분리(`SignalHistoryService:49-59,82`), avgAlpha zero-sample null. **연속순매수**: <5일 null·미포함 0 구분(`InvestorBuyStreakCalculator:35-36`). **공매도**: 전회차 P1 수정 정합(위).

### ④ §16-10 이중 인코딩 — **신규 외부호출 전수 SAFE (증명)**

전회차 P1 2건 수정 정합(위). Naver 회귀 무(URI 유지). DART 신규 소비(`DartDisclosureMonitorService→DartService`) = URL 파라미터 전부 ASCII(crtfc_key hex·corp_code 숫자·yyyyMMdd), 한글 stockName 은 corp-code 조회+Java 필터(`.contains`)뿐 URL 미포함 → 재인코딩 멱등. 저널·RVOL·streak = 외부 URL 조립 없음. **LATENT 0.**

### ⑤ 중복 구현 — **단일 출처 (증명)**

- **RVOL**: `RvolService.computeRvol`/`pastDailyValues`(순수) 단일. 스냅샷·보드·단건상세·저널 4소비처 전부 위임. 분자 `resolveTradingValue` 단일.
- **연속순매수**: `InvestorBuyStreakCalculator.consecutiveNetBuyDays`(순수) 단일, 단건·보드 벌크 공용.
- **pct/alpha**: `SignalOutcomeService`(scale 4)에서 1회 계산·저장, 표시 5작업은 **read-only**(신규 scale-divergent 사본 없음). avgAlpha 평균만 scale 2 일관. ⚠ 단 **저널 `evaluate` 는 별도 scale 2 계산**(#2 P3 — 표시작업 밖 별 기능).
- ATR/RSI: 백엔드 canonical(`AtrCalculator`/`TechnicalIndicatorService`) 위임, 재구현 0(전회차 재확인).

### ⑥ 죽은 코드/고아 — 신규분 추가 발견 없음

신규 크론 8번째(executeNxtLiquidationRetry)는 활성·의도적(flag OFF 선차단). 종가봇 2개는 여전히 의도된 휴면(@Scheduled 주석, 재설계 완료). 전회차 미제거 죽은코드(`sellAllPortfolio`·`parsePriceInfo`·finance 엔드포인트 2)는 이번 범위 밖(별도 승인 대기).

### ⑦ 백로그 정합 — **일치 (증명)**

P3-8(KIS 3토큰 캐시) 실코드 정확 일치: `KoreaInvestmentService:64`(+expires_in :300·401 :141) / `KisApiService:40`(expires_in 有·401 無) / `MarketIndicatorService:54`(expires_in 有·401 無). P2-13 재개봉→구현(flag OFF): `nxtLiquidationEnabled`/`nxtRoutingEnabled` 기본 false, `shouldRunNxtLiquidation` 둘 중 하나 OFF 시 false=현행 보존. 잘못 done 표기된 티켓 없음. (P3: 저널 티켓 부재·P2-CAT3 워밍확장 미갱신 — 추적성.)

---

## 특별 항목 (⑧⑨⑩⑪)

**⑧ Flyway V43~V45 ↔ legacy_base_stub — OK(증명).** V43(신규 테이블)·V44(V43 컬럼 after ALTER)·V45(신규 테이블) 전부 **자기완결**(baseline≤14 테이블 미접촉) → 스텁 컬럼 추가 불요. `FlywayMigrationTest` `migrate()` 성공+`pending().isEmpty()`(:72-79)로 3개 실 MariaDB clean-apply 검증됨. **P3(#4)**: V43-45 객체 타겟 단언은 없음(V36/40/41/42 만).

**⑨ 신규 크론 락/멱등/시간대 + 크론 8 단언 — OK(증명).** 실 활성 @Scheduled 8개 열거(스캘핑3·정규청산2·**NXT청산1**·스윙2) = `AutoTradingBotTrackTest` 8 단언 일치. 저널평가 19:40 KST `@Transactional` 멱등(evaluatedAt drop)·락 무(read/eval batch, SignalOutcome 형제와 동일)·SellInflight=크론 아님 TTL 60s·워밍 08:00 SchedulerLock fail-open+§4b 캐시히트 멱등·NXT청산 리더 fail-CLOSED+markLiquidatedToday 멱등. NXT cron `0 0/5 15-19` zone Asia/Seoul, 창가드 15:35 선차단(15:00~30 미발화)+flag OFF.

**⑩ 알림 폭주 — P2 1건(#1) + P3.** CatalystRiskAlert dedup 비원자(check-then-act, alert_key UNIQUE 부재) → 동시 최초분류 시 중복 발송(금전 무관). 단 **다채널·크로스경로 중복은 방지됨**(held→SIGNAL_AND_RISK 는 의도, `getCatalyst/classifyOneBatch` 가 generic 알림을 `fireTargetNegativeAlert` 뒤로 게이트). DART dedup 도 비원자나 4분락 직렬화(P3).

**⑪ NXT flag OFF = 현행 바이트 동일 — OK(증명, 독립 감사관).**
- **REGULAR 4-arg 경로**: `sellPortfolioMatching` REGULAR 분기가 **4-arg** `activeTradeService.sell`(:2759) → `RealTradeService` REGULAR 분기가 **3-arg** `kisService.sellStock`(:464) → `applyNxtRouting` effective=REGULAR 시 body **무변경** null 반환(:1347). 주문 바디 pre-NXT 와 동일.
- **fail-CLOSED ↔ killswitch 분리**: 미확정 NXT = `rt_cd="1"` ObjectNode(:1351-1358, null 아님) → RealTradeService 에서 null→killswitch 분기(:472) 스킵, rt_cd≠0 → throw(:480, **killswitch·주문 없음**). "거부"≠"불확실" 정확 분류.
- **executeNxtLiquidationRetry 게이트 전량 관통**: 리더 fail-CLOSED(:2515)→shouldRunNxtLiquidation(두 flag·configOn·botActive·미killed·창)→sellPortfolioMatching(NXT)→SELL 게이트 셋(killswitch·in-flight·KIS성공+DB실패 killswitch). **지정가**(ORD_DVSN=00 하드코딩), markLiquidatedToday 는 완청산 시만. 브레이커 정당 생략(진입 전용, 이건 청산). 우회 0.
- **종가봇 NXT 진입 없음(§16-2)**: `NXT_EXTENDED` 는 SELL/청산 코드에만 출현. BUY 는 전부 `buyStock` 3-arg(REGULAR), session-overload buyStock 4-arg 는 **프로덕션 호출부 0**. 종가매수 = `activeTradeService.buy`(무세션)+isMarketClosed(≤15:30) 상한+게이트 셋.

---

## 이번 세션 조치 = **리포트만**(코드/문서 무변경 — 감사 원칙)

P0/P1 부재라 즉시 수정 대상 없음. 수정 권장(별도 세션, 우선순위순):
1. **#1 P2** — `alert_history.alert_key` UNIQUE(마이그레이션 V46) + `CatalystRiskAlertService.recordSent` 조건부 insert(rowsAffected==1 게이트, §4d④ 브레이커 패턴 이식) → dedup 원자화.
2. **#2 P3** — 저널 `evaluate` pct/alpha scale 4 통일(공용 `pctChange(now,base,scale)` 헬퍼) → "동일 잣대" 실현(전회차 #7 이월).
3. **#3 P3** — `applyNxtRouting` javadoc "markFailed"→"SELL 경로 rt_cd≠0 throw(killswitch 미발동)"로 정정.
4. **#4 P3** — `FlywayMigrationTest` 에 `manual_trade_journal`·`bm_price_at_buy`·`bot_sell_inflight` UNIQUE 존재 단언 추가.
5. **#5~7 P3** — 저널/워밍확장 백로그 추적·Gemini patient rate 게이트·RVOL 표시 정밀도.

## 미감사/한계 (예산·환경 명시)

- 7축 전부 감사 완료(신규분). A-1 프론트는 주요 화면(허브/상세/보드) 위주. 운영 DB 필요 항목(알림 실중복 빈도·死피드 as-of)은 로컬 검증 불가 — 서버 로그 확인 권장(배포 후: `[SellInflight]` acquire/release·악재경보 중복 0·공매도 미수집 표기).
- 감사관 5병렬 + 직접 재검증(P1 3건·RVOL·타이포 커밋). NXT 코드는 저자 회피 위해 독립 감사관 전담.
