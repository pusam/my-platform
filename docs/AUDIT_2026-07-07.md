# AUDIT 2026-07-07(E) — 불변식 기준 전수 감사 리포트

> 감사 세션(신규 기능 금지·산식/임계/봇 로직/Flyway 무변경). 근거는 전부 파일:라인 — 추측 없음.
> 표기: **P0**=즉시 위험(실손실/보안) · **P1**=실버그(오동작 중이나 완화 존재) · **P2**=정합성/저위험.
> 이 세션에서 수정한 것 = P2 저위험(주석·죽은 코드·티켓 정리)만. P0/P1 은 리포트만(별도 세션에서 수정).

## 요약 (위험도순)

| # | 위험도 | 발견 | 위치 | 조치 |
|---|---|---|---|---|
| 1 | **P1** | Google 뉴스 RSS 이중 인코딩(§16-10 위반) — 매 호출 한글 포함이라 상시 오작동 | `GoogleNewsService.java:64-73` | 리포트만(수정=동작 변경) |
| 2 | **P1** | 종목 검색 네이버 폴백 이중 인코딩 — 한글 키워드 폴백 오작동 | `StockPriceService.java:708-714` | 리포트만 |
| 3 | **P1** | 공매도 비율 결측→`ZERO` 위장(§4c) — 死피드 결합으로 체크리스트가 "0.00% 충족"을 사실처럼 표시 | `ShortSellingService.java:520-530` → `BuyChecklistService`·`AutoTradingBotService:1307` | 리포트만 |
| 4 | **P2** | 종가봇(비활성) 2경로에 리더 게이트·PriceSanityGuard 부재 — 재활성 전 필수 보강 | `AutoTradingBotService.java:3073,3238` | 리포트+주석 경고 추가 |
| 5 | **P2** | `sellAllPortfolio` 죽은 코드(무호출·무게이트 public) | `AutoTradingBotService.java:2520` | 리포트(제거는 별도 승인) |
| 6 | **P2** | 수동 REAL 매수 reason="MANUAL" 고정 → 같은 날 2회째 수동 매수 멱등키 차단(과차단, fail-safe) | `PaperTradingController:356`+`RealTradeService:274` | 리포트(의도 확인 필요) |
| 7 | **P2** | 저널 평가 scale 2 vs signal_outcome scale 4 — isHit 경계(3.00%/α0)에서 두 화면 hit 판정 상이 가능 | `ManualTradeJournalService.evaluate:239-245` | 리포트 |
| 8 | **P2** | KOSPI 개장영향(getKospiImpactAnalysis) 전소스 다운 시 dataAvailable 없이 NEUTRAL 표시 | `GlobalFuturesService.java:309,514-535,817-826` | 리포트 |
| 9 | **P2** | 공매도 표시 계층 as-of/스테일 가드 부재(死피드를 현재처럼 표시) + 피드 사망 무알림 | `ShortSellingService.java:568-581` | 리포트 |
| 10 | **P2(문서)** | VERIFICATION_BACKLOG P0-1 티켓 미마감(코드는 3중 그물+테스트로 구현 완료) | `StockPriceService.java:1074-1114`+`StockPriceOutlierGuardTest` | **이번에 티켓 마감** |
| 11 | **P2(주석)** | P2-13-a 는 **이미 코드에서 정정돼 있음**(:229-239) — 대신 클래스 헤더 활성 크론 수 5≠실제 7 불일치 발견 | `AutoTradingBotService.java:83-91` | **이번에 헤더 정정 + 백로그 마감** |
| 12 | **P2** | 수급/시장지표 랭킹 화면 현재가 = 랭킹 API `stck_prpr` 직노출(단일 경로 미경유, borderline) | `KisApiService:247`외 5파일(A-1 상세) | 리포트(후처리 통일 권장) |
| 13 | **P2** | 프론트 고아 컴포넌트 2(PageHeader·WidgetSettingsModal) + 죽은 `parsePriceInfo`·finance 엔드포인트 2 | A-6 상세 | **컴포넌트 2 이번에 제거**, 나머지 리포트 |

---

## Part A-3. §4d fail-closed 호출그래프 (최우선 축 — 증명 완료)

**결론: 활성 주문 경로에 구멍 0. KIS 주문 도달 경로는 전수 `RealTradeService` 단일 관문.**

- KIS 주문 = `KoreaInvestmentService.buyStock`(:1171)/`sellStock`(:1194)→`placeOrder`(:1211). 프로덕션 호출부는 **각 1곳뿐**: `RealTradeService.executeBuy:297` / `RealTradeService.sell:437`. 다른 클래스 직호출 없음(전수 grep).
- **REAL BUY**: 스윙(`executeSwingBuyLogic:2674`) = 리더(:2676)→봇활성/kill(:2677)→자산킬스위치(:2682)→**브레이커**(:2689)→**PriceSanity**(:2770)→RealTradeService[DB킬스위치+한도+실잔고+멱등키(:274)]. 스캘핑은 REAL 하드 return(:1353)=VIRTUAL 전용. 수동(`/real/trades`, ADMIN)= 킬스위치+한도+잔고+멱등키(리더/브레이커/PS 미적용은 의도 — 운영자 판단).
- **REAL SELL**(브레이커 미게이트 = 스펙 일치): 스캘핑(:2008)·스윙(:2932)·청산(:2372)·정규장 강제청산(:2413, SELL-only 확인) 전부 리더 게이트 + RealTradeService DB킬스위치(`checkSell`) 통과.
- **게이트 극성 검증**: 리더 = Redis 예외 시 `leader=false`(fail-CLOSED, `BotLeaderElectionService:106-110`) ✓ / DB킬스위치 = 예외 전파→주문 abort(fail-CLOSED, `TradingSafetyService:75-85` no-catch) ✓ / 브레이커 = **BLOCKED-before-null 유지**(`DailyLossBreakerService:98-106`) + 미발동 조회실패만 fail-open(:140-146, :581-584) = 문서화된 의도 ✓ / BUY 멱등키 = DataIntegrityViolation 만 SKIP, 그 외 DB 오류 전파=abort(fail-CLOSED) ✓.
- **ATR V42 REAL 2중 하드 가드 실배선 확인**: `isAtrSetActive:479-481` = `enabled && mode==VIRTUAL`(REAL 항상 false) ✓, `resolveSwingExitLevels:516-522` = REAL 항상 -3/+5 ✓. `PositionSizer.judge`(:1522,:2787)·`AtrExitRule.judge`(:2784) 전부 `isAtrSetActive` 블록 안 — REAL 우회 사용처 0 ✓.
- `reconcilePositionsWithKis:742-780` = read-only(주문 호출 없음) ✓. `VirtualTradeService` = KIS 도달 경로 0(grep 무매치) ✓.

**구멍(전부 비활성 경로 — 요약표 #4/#5)**:
- `executeClosingBuyLogic:3073`(@Scheduled 주석=휴면): 리더 게이트·PriceSanity **없음**(브레이커·킬스위치·멱등키는 있음). 재활성 시 전 인스턴스 실행+오염가 진입 미차단.
- `executeClosingSellLogic:3238`(휴면): 리더 게이트 없음. SELL 멱등키 부재라 이중 시도 가능(실잔고 체크+킬스위치가 반경 제한).
- `sellAllPortfolio:2520`: public·무게이트·무호출(주석 :2621 "sellPortfolioMatching 로 대체"). 죽은 코드 — 오용 방지 위해 제거/게이트 권장(제거는 봇 파일이라 이번 세션 범위 밖, 별도 승인).

## Part A-2. §4c 결측 위장 + "조용한 죽음"

**P1(요약표 #3) — 공매도 비율 결측→ZERO**: `ShortSellingService.getShortSellingRatio:520-530` 이 데이터 전무(:521)/종목 미포함(:527)/조회 예외(:530) 전부 `BigDecimal.ZERO` 반환.
- `BuyChecklistService:107-113` → 체크리스트에 `"0.00%" passed=true` 렌더 = **미수집을 "공매도 0% 충족"으로 위장**(§4c 위반). 07-07(B) 진단으로 피드가 **死**(KRX LOGOUT+네이버 404) 확정이라 사실상 전 종목이 이 위장 값.
- `AutoTradingBotService:1307-1309,1496` `isHighShortSellingStock` false → 진입 미차단. **단 이 방향 자체는 PriceSanityGuard "결측=UNKNOWN=통과" 선례와 일치**(결측 근거 차단 금지) — 문제는 차단 여부가 아니라 **ZERO 가 '실측 0%'와 구분 불가**한 것. 권장: null 반환 + 체크리스트 "미수집" 표시(pass 아님) + 봇은 현행 통과 유지.
- 파생(#9): `getTopShortSellingStocks:568-573`/`getStockShortSellingHistory:580-581` 는 `findLatestTradeDate` 행을 **나이 무관** 현재처럼 서빙(as-of 미노출), 수집기 사망은 `log.warn` 뿐(무알림).

**P2(#8) — Yahoo 개장영향**: `GlobalFuturesService.getKospiImpactAnalysis`(:309 초기값 50.0, :514-535) — 전 소스 다운이면 요인 0개 합산=50 유지 → "NEUTRAL·보합 출발 예상" 표시, `dataAvailable` 플래그 부재(같은 데이터의 `OvernightUsMarketService` 는 하드닝 완료 — 비대칭). F&G 실패 폴백(:817-826)도 score 50/neutral 채움(단 `success:false`+"데이터 없음" 라벨 동반 = borderline).

**OK 판정(전수 확인)**: DART(오류 시 미영속 — "0건 공시" 위장 없음, `DartService:224-228`·`EarningsDisclosureService:155-158`) / ECOS(`EcosClient:83-103` 오류=empty→축 null, 문서화된 의도) / KIS 수집기(실패일 스킵, zero-flow 미영속, `KisInvestorDataCollector:203-214`) / **재료 §4c 하드닝 classifyBatch 후에도 intact**(`StockCatalystService:76-79`(단건)·`:147`(배치) isAvailable 게이트 → 미캐시) / 신규 코드(저널·보드·streak·주간집계·MacroTilt) 전부 §4c 준수 모범.

경미: `SectorTradingService:464,512-514`(빈 섹터 changeRate 0% 표시 — resolveAccumulatedValue 실측-only 는 intact), KIS 응답 내 개별 필드 결측→0(`KisInvestorDataCollector:470-483`).

## Part A-4. §16-10 이중 인코딩 (외부 HTTP 전수)

- **P1 BUG 2건**(요약표 #1,#2 — 2026-07-01 네이버 사건과 동형, 미이관):
  - `GoogleNewsService.java:64-73` — `URLEncoder.encode(stockName+" 주식")` 후 **String** 을 `exchange` 에 전달 → RestTemplate 재인코딩(%→%25). `" 주식"` 상수 때문에 **매 호출 이중 인코딩**(구글이 관대해 결과가 좋아 보일 수 있으나 쿼리 자체가 `%25EC%2596%25...` 로 나감 — 직접 검증 시 raw 로그 확인).
  - `StockPriceService.java:708-714` `searchFromNaver` — 동일 패턴, DB 0건 폴백 경로의 한글 키워드 검색 오작동.
  - 수정 방향(다음 세션): 네이버 수정과 동일 — `UriComponentsBuilder...encode(UTF_8).build().toUri()` 로 **URI 전달**(+회귀 테스트, `NaverSearchServiceTest` 패턴).
- **SAFE 확인**: NaverSearchService(URI 유지 — 회귀 무), Telegram(한글은 JSON body — URL 아님), ECOS(URI), DART/KIS/Gemini/Yahoo/KIND/python 내부(전부 ASCII 또는 body). LATENT 0.

## Part A-5. 중복 구현

- **문서화된 미러 = 전부 일치 검증**: python `metrics.is_hit`(임계·부등호 Java `isHit` 와 정확 일치), `wilder_rsi`/`wilder_atr`/`atr_rule_prices`(백테스트 전용, Java 와 정의 일치·표면 비중첩). RSI/ATR 백엔드 사용처는 전부 canonical(`TechnicalIndicatorService`/`AtrCalculator`) 위임 — 재구현 0. 프론트 자체 RSI 없음(`useChartCalculations` 는 좌표 계산만).
- **DUPLICATE-RISK(P2, #7)**: pct/alpha 산술이 `SignalOutcomeService`(scale 4) vs `ManualTradeJournalService.evaluate`(scale 2) — 같은 `isHit` 에 다른 정밀도 입력 → 경계(pct 2.9999→3.00 반올림, α≈0)에서 신호 이력 vs 내 저널 화면의 hit 판정이 갈릴 수 있음. 권장: 공용 `pctChange(now, base, scale)` 헬퍼 + 저널도 scale 4 (fix는 산식 결과값 변경이라 이번 세션 범위 밖).
- 5일 등락률 사본 2곳(`RecommendationService:1919` double / 저널 :406 scale2) — 표면 비공유라 저위험, 공용화 후보.

## Part A-1. 단일 시세경로

**결론: 주 표시 경로(허브/관심종목/추천/상세/저널/보드) 하드 위반 0.** Redis/L1 가격 직접 read 0(경로 문서 :376-378 그대로), 프론트 자체 현재가 계산 0(상품 페이지 제외), python 은 per-stock 현재가 서빙 자체가 없음(grep 0건). 신규 코드(저널 :190·보드 :248·RVOL :48) 전부 단일 경로 준수.

**BORDERLINE(P2) — 수급/시장지표 벌크 랭킹 화면의 현재가 컬럼**: 랭킹 API 응답의 `stck_prpr` 를 그대로 DTO `currentPrice` 로 노출(단일 경로 미경유·비정합 가능) — `KisApiService:247,273,367` · `MarketIndicatorService:271,276` · `InvestorTradeService:150,157` · `InvestorSurgeService:275-277` · `InvestorDailyTradeService:256` · `KisInvestorDataCollector:255,418`. 성격상 "랭킹 쿼리가 낸 스냅샷 컬럼"이라 병렬 시세 *경로* 신설과는 다르나, 화면 간 가격 불일치의 잠재 원인. 권장(선택): `AiStrategySnapshotService:542-564` 가 이미 쓰는 패턴(리스트 조립 후 `getStockPricesFromCacheOnly` 로 덮어쓰기)으로 후처리 통일. 파생: `StockFinancialDataCollector` 가 저장한 가격을 턴어라운드 스크리너가 폴백 표시(`QuantScreenerService:1019`) — 스테일 가능. `StockDetailService.parsePriceInfo(539-564)` 는 **무호출 죽은 코드**(지뢰 — 회귀 방지 위해 제거 후보, 상세 실경로는 :97 getStockPrice).

## Part A-6. 죽은 코드·주석 불일치

- **P2-13-a — 이미 정정돼 있음 확인**: 현행 주석 `AutoTradingBotService:229-239` 가 "실 binding=isMarketClosed()(15:30), 08~20 윈도우=死코드"를 명시(매도 내부 호출 :2031/:2948). 백로그 티켓만 이번에 마감. 가드 일원화는 NXT 배선 시 잔여.
- **신규 발견(강함) — 클래스 헤더 활성 크론 수 불일치**: `AutoTradingBotService:83-91` 이 "활성 5"로 열거하나 실제 활성 @Scheduled 는 **7**(강제청산 :2413 + 미완료경고 :2456 누락) — `AutoTradingBotTrackTest.exactlySevenScheduled` 는 7 을 단언(카운트는 가드되나 산문은 드리프트). **이번에 정정**(5→7, 두 메서드 열거 추가).
- 죽은 @Scheduled: 종가봇 2개뿐(의도된 휴면 — 가드 테스트 존재, 유지). 죽은 메서드: `sellAllPortfolio:2520`(무호출·무게이트, `sellPortfolioMatching` 로 대체 주석 :2621 — 제거는 봇 파일이라 별도 승인).
- **프론트 고아 컴포넌트 2 — 이번에 제거**: `PageHeader.vue`(자기 참조뿐)·`WidgetSettingsModal.vue`(참조 0). vitest 181 + build green 으로 무영향 확인.
- 미사용 엔드포인트: **확실 죽음** = `GET /api/finance/recurring/active`·`/month`(`RecurringFinanceController:56,65`, 프론트/문서/py 참조 0 — 주식 외 도메인이라 이번 세션 미제거, 제거 권장). ops-plausible(유지) = KIS 디버그 3종(`/api/kis/*` — `/api/investor/*` 로 대체된 상태), `/api/analysis/supply-demand·technical/{code}`(diagnosis 부분집합), signal-outcomes ops 4종(permitAll, curl 진단용 문서화), `/investor/conviction`(api.js:929 에 "백엔드 보존" 명문), trigger-buy·breaker·strong-value-frequency·kospi-daily(python 내부 소비) 등.
- 기타 spot-check(SchedulingConfig·RecommendationService·StockCatalystService 등): 주석-동작 불일치 추가 발견 없음(Recommendation 의 "죽은 코드" 주석들은 자기 인지형 — 정확).

## Part A-7. VERIFICATION_BACKLOG 잔여 티켓 소진

| 티켓 | 판정 | 근거/재개봉 조건 |
|---|---|---|
| **P0-1** | ✅ **이번에 마감** — 구현 완료 확인 | 그물1 밴드(:1082-1090)+그물2 ±31%(:1094-1099)+그물3 DB앵커 5×/0.2×(:1105-1114) + `StockPriceOutlierGuardTest` 존재. 티켓만 미마감이었음 |
| P3-1 | **전진** — 잔여 ③ 설계 완료 | `docs/DESIGN_P3-1_IDEMPOTENT_ORDERS.md`(본 세션). 구현은 다음 세션 |
| P2-CAT2 | 보류(구현 티켓 — 감사 세션 범위 밖) | quota 압박 실측 후 |
| P3-3 | 보류 | V29 백필 비용 大, verdictFor 가드로 1차 방어 중 — 신규 소비처 추가 시 재검토 |
| P3-4 | 보류 | P2-12 결론 확정이라 실익 낮음 — 차트 타이밍 재개 결정 시 |
| P3-5 / P3-7 | **데이터 대기**(검증 불가) | 스냅샷 축적 8~12주(V39 는 2026-07-06 시작) 후 KOSPI 방향 대조 |
| P3-6 | **로컬 검증 불가** | 발생 빈도 스캔은 운영 DB 필요(로컬 mariadb 미기동/empty). 서버에서 `ABS(change_rate)>31` 스캔부터 |
| P1-6 가중치 | 데이터 대기 | `SignalWeeklyAccuracy` 주간 스냅샷 추세(n 확보 후 A안 재검토) |
| P2-17 | 데이터 대기 | VIRTUAL flag ON 2주+ 실측(BotPerformance+감사로그) |
| P2-13 | 재개봉 대기 | 2026-09-14 NXT 규격 + sellStock 세션 파라미터 선결 |
| P2-16 잔여 | **로컬 검증 불가** | 서버에서 `docker compose pull backend` 후 python-health chart-sector 변동 확인 |

## 이번 세션 수정 목록 (P2 저위험만 — 전부 주석/문서/죽은 코드, 동작 무변경)

1. `AutoTradingBotService` 클래스 헤더 활성 크론 수 정정(5→7, 강제청산·경고 메서드 열거 추가).
2. 종가봇 휴면 2경로(`executeClosingBuyLogic`/`SellLogic`)에 "재활성 전 리더 게이트(+매수는 PriceSanity) 필수" 경고 주석.
3. VERIFICATION_BACKLOG: P0-1 ✅ 마감(3중 그물+테스트 근거) · P2-13-a 주석 정정 완료 확인 표기.
4. 프론트 고아 컴포넌트 제거: `PageHeader.vue`·`WidgetSettingsModal.vue`(참조 0 재검증, vitest 181+build green).
5. `docs/DESIGN_P3-1_IDEMPOTENT_ORDERS.md` 신규(Part B) + STOCK_AZ_FULL §20 인덱스 추가.

**리포트만(다음 세션 수정 대상)**: #1,#2 이중 인코딩(URI 이관+회귀 테스트) · #3 공매도 ZERO→null+체크리스트 "미수집" 표기 · #8 개장영향 dataAvailable · #7 저널 scale 4 통일 · #12 랭킹 현재가 후처리 · `sellAllPortfolio`/`parsePriceInfo`/finance 엔드포인트 2 제거.

## 미감사 축 (예산 제약 명시)

- **없음 — 7축 전부 감사 완료.** 단 축별 한계: A-1 프론트는 주요 화면(허브/상세) 위주 light check, A-6 DTO 미사용 필드는 상위 3~5 만 spot-check, A-7 중 P3-6(운영 DB 스캔)·P2-16 잔여(서버 이미지 확인)는 **로컬에서 검증 불가**(사유 상기 표).
