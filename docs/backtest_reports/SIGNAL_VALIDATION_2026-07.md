# 시그널 검증 종합 리포트 — 2026-07

> **성격**: 측정 전용 백테스트(Phase 1~3) 결과 종합 + **권고까지만**.
> 산식·임계·봇 상수 변경은 이 문서가 아니라 **사람이 별도 세션에서 결정**한다.
> 원데이터: `composite_backtest_2026-07-06.json` · `supply_hypothesis_2026-07-07.json` · `exit_backtest_2026-07-07.json`

## 공통 설정

- 유니버스 139종목(SectorStockConfig 스냅샷) × 2026-01-02~06-30, 전부 point-in-time(`df.loc[:D]` 가드).
- 진입 D+1 시가 → +3거래일 종가(Phase 3 은 규칙별 청산), 비용 = 수수료 0.03% + 세금 0.18% + 슬리피지 0.15%.
- hit 정의 = 운영 `SignalOutcome` 미러(alpha≥0 AND pct>0, alpha 없으면 pct≥3%).
- KOSPI BM = Yahoo ^KS11(로컬 실행, Java KIS 폴백 순서), 수급 = 네이버 frgn 크롤(수량×종가 근사).

## Phase 1 — 종합점수 4카테고리 예측력 (16,680 신호)

| 카테고리 | 측정 | 강세 기준 | 강세 hitRate / avgNet | 약세 hitRate / avgNet | Spearman(점수↔수익) | 판정 |
|---|---|---|---|---|---|---|
| 기술 | ✅ | ≥13 | **33.5% / +0.47%** | 30.8% / -0.23% | 0.02 | **임계 게이트로 유효**, 연속 점수로는 무의미 |
| 수급 | ✅ | ≥15 | **29.7% / -0.30%** | 31.5% / -0.07% | 0.00 | **역상관 독립 재확인** — 강세가 약세보다 나쁨 |
| 실적 | ❌ 미측정 | — | — | — | — | 가격으로 재현 불가(운영 스냅샷 CSV 필요) |
| 섹터테마 | ❌ 미측정 | — | — | — | — | 상동 |

- 수급 밴드 상세: **5-9 밴드가 최고**(hitRate 33.6%, avgNet +0.20%) — "적당한 수급"이 극단(15+: 29.7%)보다 낫다.
  prod 실측(n=88)의 단조 역상관과 방향 일치 — **P1-6 캡(10) 방어 타당성 보강**.
- 기술 15-20 밴드만 avgNet 양수(+0.59%) — 게이트(≥13)는 유효하되 점수 크기에 추가 정보 없음(Spearman ≈ 0).

## Phase 2 — 가설 A: 수급 "연속 순매수일" vs "금액" (수급 신호 정의 비교)

| 변형 | n | hitRate | avgAlpha | avgNet | PF | 판정 |
|---|---|---|---|---|---|---|
| **streak5** (합산 순매수 5일 연속 도달) | 378 | **33.1%** | **-0.55** | **+0.07%** | **1.02** | 4개 중 유일하게 avgNet 양수·PF>1 |
| amount50 (당일 순매수 ≥50억) | 3,677 | 32.0% | -0.88 | -0.30% | 0.91 | |
| streak3 | 1,027 | 29.9% | -1.15 | -0.31% | 0.90 | |
| current_formula_strong (현행 미러 ≥15) | 701 | 29.7% | -1.09 | -0.30% | 0.90 | **최하위** |

**결론**: "연속일(5일) > 금액" 가설을 **약하게 지지** — 단 streak5 도 alpha 는 음수(-0.55)라 "수급 단독 신호가 초과수익을 낸다"는 아니고, "현행 강세(금액 위주 ≥15)보다 덜 나쁘다" 수준. 어느 변형도 산식 편입 근거로는 부족.

## Phase 3 — 가설 B: ATR 동적 청산 vs 고정 -3%/+5% (n=3,640, 기술≥13 proxy 신호셋)

| 규칙 | avgNet | 승률 | PF | 평균보유일 | -5% 초과 손실 비중 | 10종목 포트 MDD |
|---|---|---|---|---|---|---|
| 고정 -3/+5 (현행 PLAN_* 미러) | -0.22% | 39.3% | 0.89 | 2.15 | **0.0%** | 90.6 |
| ATR×1.5 | +1.47% | 50.1% | 1.39 | 5.75 | 40.2% | 36.8 |
| ATR×2.0 | +1.88% | 51.3% | 1.44 | 7.17 | 38.3% | 34.3 |
| **ATR×2.5** | **+2.10%** | **52.0%** | **1.47** | 8.05 | 35.1% | 36.7 |

**결론**: 사전 룰("고정이 이기면 현행 유지")과 달리 **ATR 이 전 변형에서 우위** — 고정 -3% 는 정상 변동성에 너무 자주 걸려 손절 빈도 60%(ATR×2.5 는 24%). 단 트레이드오프가 뚜렷하다:
- ATR 은 개별 트레이드 최대손실이 크다(worstTrade -30% 대, -5% 초과 손실이 트레이드의 35~40% — 고정은 0%).
- 신호셋이 기술≥13 proxy(운영 STRONG_BUY/BUY 아님)라 운영 매매와 모집단이 다르다.
- 봇 상수 변경 시 `StockConclusionService.PLAN_*` 동기 불변식 + 일일 손실 서킷브레이커(V38) 상호작용 검토 필요.

## Phase 3-b — 포트폴리오 레벨: 고정 vs ATR세트(×2.5 청산 + 리스크 균등 사이징) (2026-07-07 추가)

> ATR 세트 도입(VIRTUAL 전용·flag `bot.atr-trading.enabled`, `docs/ATR_TRADING_SET.md`)의 사전 검증.
> **핵심 질문 = "세트가 일일 손실 브레이커(V38) 발동을 늘리지 않는가."**
> 스윙 봇 미러(자본 50만·종목당 50%·2슬롯·5일 타임컷) 일 단위 재생, 브레이커 한도 30만원 가상 재현.
> 원데이터: `portfolio_exit_backtest_2026-07-07.json`.

| 지표 | 고정 -3/+5 (현행) | ATR세트 (×2.5 + riskBudget 5만) |
|---|---|---|
| **브레이커 가상 발동** | **0회** | **0회** — 핵심 질문 충족(동수) |
| 총수익 (1~6월) | -70.9% | **+96.3%** |
| MDD (equity) | 72.7% | **18.0%** |
| 일일 최대 실현손실 | -193,420원 | **-73,984원** |
| 트레이드 / 승률 / 평균보유 | 377건 / 31.8% / 1.5일 | 67건 / 59.7% / 4.6일 |
| 청산 구성 | 손절 67% | 손절 7% / 타임컷 79% |

- **해석**: 고정 -3%는 5일 타임컷 체제에서 손절 빈도 67% + 재진입 churn(377건 × 왕복비용 0.36%)이
  수익을 갉아먹는 구조가 포트폴리오 레벨에서 증폭. ATR 세트는 넓은 손절로 churn 이 줄고
  보유가 길어져(4.6일) 비용·손절 모두 감소. **일일 최대 실현손실도 세트가 더 작음** —
  "개별 손실 확대" 우려가 포트폴리오 레벨에선 사이징·저회전으로 상쇄됨을 시사.
- **추가 한계(위 공통 한계에 더해)**: ① 트레일링 스탑(+2% 후 -2%) 미반영(일봉 근사 불가, 양쪽 동일) —
  실제 봇에선 고정 변형이 트레일링 이득을 더 봄. ② **재진입 쿨다운 미반영** — 실봇 스윙은 매도 종목
  재진입을 막아 fixed 의 churn 이 이보다 작을 수 있음(방향은 불변, 크기 과대 가능). ③ proxy 신호셋.
- **결론**: 브레이커 발동 기준 세트 도입의 안전 조건 충족. **REAL 확장은 VIRTUAL 2주+ 실측 후 별도 결정**
  (조건은 VERIFICATION_BACKLOG P2-17).

## 권고 (산식 변경은 전부 별도 세션에서 사람이 결정)

1. **기술**: 임계 게이트(≥13) **유지**. 연속 점수 가중 강화는 근거 없음(Spearman 0.02).
2. **수급**: composite 캡(10, P1-6) **유지** — Phase 1·2 모두 강세(≥15)가 약세보다 나쁨을 재확인.
   `scoreSupplyDemand` 를 금액 중심 → **연속일(streak5) 반영으로 재설계할 가치는 있음**(후보 과제) —
   단 streak5 도 alpha 음수라 기대치를 낮게, `SignalWeeklyReportService` 주간 데이터와 교차 확인 후.
3. **실적·섹터테마**: **미측정 상태를 명시적으로 유지**(§4c — 미측정을 "문제없음"으로 위장하지 않는다).
   운영 `RecommendationSnapshot` 보존이 7일이라, 측정하려면 주기적 CSV export
   (`GET /api/admin/backtest-export/recommendation-snapshots`) 축적이 선행되어야 함.
4. **청산 규칙**: ATR×2.0~2.5 가 유의한 개선 후보 — 곧바로 봇 교체가 아니라
   **① 모의(VIRTUAL) 모드에서 ATR 청산 병행 측정 → ② 운영 신호셋(total≥55)으로 재검증**(스냅샷 CSV 확보 후)의
   2단계 검증을 권고. 개별 손실 확대(-5% 초과 35%)를 감내할지는 운영자 위험선호 판단.
5. **RVOL(V41)**: 이번에 스냅샷 축적을 시작했으므로, n 이 쌓이면 "RVOL 밴드 × 적중률" 조건부 집계를
   `accuracy-by-band` 패턴으로 추가 검증(승격 조건 동일 — 데이터 검증 전 산식 미편입).

## 한계 (해석 시 반드시 인지)

- **생존편향**: 유니버스가 현재 SectorStockConfig 스냅샷(상폐·편출 종목 부재, P3-4 ticker_list 미해결).
- **모집단 상이**: 운영 `signal_outcome` 은 top10 랭킹 통과 신호만, 본 백테스트는 전 종목·전 거래일 무조건부 —
  절대 수치(hitRate ~30%)를 운영 적중률과 직접 비교하지 말 것.
- **수급 근사**: 네이버 순매매량×종가(정밀 금액 아님). `--flows-csv` 주입 시 정밀화 가능(export 미구현).
- **자기상관**: 동일 종목 연속 거래일 신호는 독립 표본이 아님(중첩 보유창) — n 이 실효 표본보다 커 보임.
- **스냅샷 보존 7일**: 실적·섹터 측정과 total≥55 신호셋 정밀화는 장기 export 축적 전까지 불가.
- **Phase 3 신호셋**: 기술≥13 proxy — composite total 은 실적/섹터 없이 계산 불가(validCount≥3)라 정직한 대용.
