fastapi==0.115.0
uvicorn[standard]==0.30.0
pykrx==1.0.45
redis==5.0.0
pydantic-settings==2.4.0
pandas==2.2.0
requests>=2.28  # Java KIS 지수 엔드포인트 호출(index_source) — pykrx 통해 이미 존재하나 명시
setuptools<81  # pykrx 가 pkg_resources 사용 — setuptools 81+ 에서 제거됨
pytest==8.3.0  # 지표 순수함수 단위테스트(차트기법) — python-backend 첫 테스트 인프라
