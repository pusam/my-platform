# 디자인 시스템 현황 · 통일안

> **작성**: 2026-08-25. **코드는 한 줄도 수정하지 않았다** — 지금 실제로 쓰이는 값을 측정해 적고,
> 어긋나는 곳에 통일안을 제안하는 문서다. 적용은 별건으로 판단한다.
>
> **범위**: `frontend/src` 의 Vue 3 컴포넌트 + `assets/css/common.css`.
> 백엔드·python-backend 는 화면이 없어 대상 아님.

---

## 0. 한 줄 요약

토큰은 **72개**나 정의돼 있는데 실제 코드는 **하드코딩 색상 2,449회 vs 토큰 참조 960회** —
**약 72% 가 토큰을 우회**한다. 문제는 토큰이 없어서가 아니라, **있는데 안 쓰는 것**이다.
상위 하드코딩 색상 대부분이 이미 정의된 토큰과 같은 값이다.

---

## 1. 색

### 1-1. 정의된 토큰 (`common.css :root`)

| 갈래 | 토큰 |
|---|---|
| 브랜드 | `--primary-start #818cf8` · `--primary-end #a78bfa` · `--primary-light #a5b4fc` |
| 표면 | `--bg-page #141422` · `--bg-white #1e1e32` · `--bg-surface #252540` · `--bg-elevated #2a2a45` |
| 텍스트 | `--text-primary #f0f0f5` · `--text-secondary #b0b0c8` · `--text-muted #7878a0` · `--text-light #55557a` |
| 상태 | `--success #34d399` · `--warning #fbbf24` · `--danger #f87171` · `--info #60a5fa` |
| 등락 | `--stock-up #f87171` · `--stock-down #60a5fa` · `--stock-neutral #9ca3af` |
| 매매 신호 | `--signal-strong-buy #ef4444` · `--signal-buy #f87171` · `--signal-neutral #a3a3a3` · `--signal-sell #60a5fa` · `--signal-strong-sell #3b82f6` |
| 귀금속 | `--gold-primary #fbbf24` · `--silver-primary #d1d5db` |

> ⚠ **등락색과 신호색은 의도적으로 분리돼 있다** — 한국 관례(매수=빨강/매도=파랑)를 따르되,
> "높을수록 좋음" 품질 스케일은 `--success`(초록) 계열을 쓴다. `common.css` 주석에 명시돼 있다.
> **이 분리를 "중복"으로 보고 합치지 말 것.**

### 1-2. 실측 — 상위 하드코딩 색상

| 값 | 횟수 | 이미 있는 토큰 | 판정 |
|---|---|---|---|
| `#fff` | 162 | `--text-white` | 토큰 우회 |
| `#ef4444` | 150 | `--signal-strong-buy` | 토큰 우회 |
| `#3b82f6` | 103 | `--signal-strong-sell` | 토큰 우회 |
| `#f87171` | 86 | `--danger` / `--stock-up` / `--signal-buy` | 토큰 우회 |
| `#888` | 77 | **없음** | **표류** |
| `#f59e0b` | 72 | `--gold-secondary` | 토큰 우회 |
| `#22c55e` | 61 | **없음** (`--success` 는 `#34d399`) | **표류** |
| `#4ade80` | 59 | **없음** | **표류** |
| `#9ca3af` | 50 | `--stock-neutral` / `--silver-secondary` | 토큰 우회 |
| `#fbbf24` | 47 | `--warning` / `--gold-primary` | 토큰 우회 |
| `#60a5fa` | 47 | `--info` / `--stock-down` | 토큰 우회 |
| `#666` | 38 | **없음** | **표류** |

**두 종류의 문제가 섞여 있다.**
- **토큰 우회**: 값은 맞는데 `var()` 를 안 쓴다 → 나중에 팔레트를 못 바꾼다.
- **표류**: 토큰에 없는 값이 자생했다 → 초록이 `#34d399`/`#22c55e`/`#4ade80` 세 갈래, 회색이
  `#888`/`#666` 로 갈라져 있다.

### 1-3. 통일안 (제안)

1. **표류 4색을 먼저 정리한다** — 이게 실제로 보이는 불일치다.
   - 초록: `--success #34d399` 하나로. `#22c55e`·`#4ade80` 는 여기로 흡수.
   - 회색: `--text-muted #7878a0` / `--text-light #55557a` 로 흡수. `#888`·`#666` 은 보라 기운이
     없어 어두운 배경에서 튄다.
2. **토큰 우회는 새 코드부터** 막는다. 2,449곳 일괄 치환은 위험 대비 이득이 낮다 —
   화면별로 손댈 때 그 파일만 정리하는 쪽이 안전하다.
3. `#fff` 는 예외로 둬도 된다(순백은 의미가 고정).

---

## 2. 타이포

### 2-1. 실측 — font-size 분포

```
12px(214)  13px(188)  11px(174)  14px(156)  16px(61)  10px(58)
15px(45)   18px(35)   9px(30)    11.5px(29) 20px(23)  12.5px(23)
```

**정의된 크기 토큰이 하나도 없다.** `--header-title-size: 22px` · `--header-subtitle-size: 13px`
두 개가 헤더 전용으로 있을 뿐이다. 그 결과 9~20px 사이가 **12단계 이상**으로 잘게 쪼개졌고,
`11.5px`·`12.5px` 같은 반픽셀까지 나왔다.

### 2-2. 통일안 (제안)

실측 빈도 상위가 곧 사실상의 스케일이다. 그걸 토큰으로 승격하고 나머지를 흡수한다.

| 토큰 | 값 | 용도 | 흡수 대상 |
|---|---|---|---|
| `--fs-xs` | 10px | 배지·각주 | 9px |
| `--fs-sm` | 11px | 보조 라벨 | 11.5px |
| `--fs-base` | 12px | 본문 기본 | 12.5px |
| `--fs-md` | 13px | 본문 강조 | — |
| `--fs-lg` | 14px | 카드 제목 | 15px |
| `--fs-xl` | 16px | 섹션 제목 | — |
| `--fs-2xl` | 20px | 화면 제목 | 18px |

> 반픽셀(`11.5`/`12.5`)은 브라우저·배율에 따라 다르게 반올림돼 줄맞춤이 깨진다. 없애는 게 맞다.

### 2-3. 폰트 패밀리

전역은 `body` 한 곳에서만 정의된다:
```
-apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', sans-serif
```

⚠ **한글 폰트가 스택에 없다.** 윈도우에선 라틴은 Segoe UI, 한글은 맑은 고딕으로 글자마다 갈라지고,
자간(`letter-spacing`)을 주면 벌어져 보인다 — 2026-08-25 관제실에서 실제로 발생한 문제다.

**통일안**: 스택에 한글 폰트를 명시한다.
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI',
             'Apple SD Gothic Neo', 'Malgun Gothic', 'Noto Sans KR', sans-serif;
```

모노는 4갈래로 갈라져 있다 — `var(--cr-mono)`(39) · `monospace`(26) · `'Monaco', monospace`(13) ·
`'Monaco','Consolas', monospace`(7). **`--font-mono` 토큰 하나로 통일**하고 한글 폴백을 포함할 것
(`'JetBrains Mono', Consolas, 'D2Coding', 'Malgun Gothic', monospace`).

---

## 3. 간격

정의된 토큰이 **이미 일관적이고 잘 쓰이고 있다** — 여기는 손댈 게 없다.

```
--spacing-xs 8px · --spacing-sm 12px · --spacing-md 20px · --spacing-lg 28px · --spacing-xl 40px
--content-padding 24px · --section-gap 24px · --card-padding 24px
--content-max-width 1200px · --gnb-height 48px · --header-min-height 60px
```

---

## 4. 라운드

### 4-1. 실측

```
8px(183)  12px(127)  10px(119)  6px(98)  4px(72)  20px(59)  16px(45)  3px(18)
```

정의된 건 `--card-radius: 16px` · `--surface-radius: 12px` 둘뿐인데 **실사용은 8단계**다.
가장 많이 쓰이는 `8px` 에 토큰이 없다.

### 4-2. 통일안 (제안)

| 토큰 | 값 | 용도 | 흡수 |
|---|---|---|---|
| `--radius-sm` | 4px | 배지·태그 | 3px |
| `--radius-md` | 8px | 버튼·입력 (**최다 사용**) | 6px, 10px |
| `--radius-lg` | 12px | 패널 (`--surface-radius` 유지) | — |
| `--radius-xl` | 16px | 카드 (`--card-radius` 유지) | 20px |

---

## 5. 그림자

`box-shadow` 총 **219회** 중 토큰(`var(--card-shadow)`) 사용은 **13회뿐**이다. 나머지는 전부 하드코딩:

```
0 10px 40px rgba(0,0,0,.3)      (8)
0 4px 6px  rgba(0,0,0,.1)       (7)
0 4px 20px rgba(0,0,0,.3)       (7)
0 4px 20px rgba(0,0,0,.08)      (4)
0 20px 60px rgba(0,0,0,.5)      (4)
0 4px 14px rgba(129,140,248,.35) (4)   ← 브랜드색 글로우
```

**문제**: 불투명도가 `.08` 부터 `.5` 까지 흩어져 있어 같은 층위의 카드끼리 깊이가 다르게 보인다.

**통일안**: 3단계로 접는다.
| 토큰 | 값 | 용도 |
|---|---|---|
| `--shadow-sm` | `0 4px 6px rgba(0,0,0,.15)` | 인라인 요소 |
| `--shadow-md` | `0 4px 20px rgba(0,0,0,.25)` | 카드 (기존 `--card-shadow` 와 동일 계열) |
| `--shadow-lg` | `0 10px 40px rgba(0,0,0,.35)` | 모달·드롭다운 |
| `--shadow-glow` | `0 4px 14px rgba(129,140,248,.35)` | 브랜드 강조 (별도 유지) |

---

## 6. 예외 — 판정 관제실 `--cr-*`

`/control-room` 은 **의도적으로 별도 팔레트**를 쓴다(`ControlRoomView.vue` 의 `.control-room` 스코프).

- 목업(네온 마젠타/바이올렛)에서 온 계기판 톤이라 앱 본문 톤과 다르다.
- **`:root` 로 올리면 전역 토큰을 덮어써 다른 화면이 깨진다** — 실제로 그 사고를 겪고 스코프로 격리했다.
- 상세: `docs/CONTROL_ROOM.md` §8, `CLAUDE.md` §7.

**이 예외는 관제실 하나로 끝낸다.** 새 화면이 자기 팔레트를 갖고 싶어지면 그건 대개 전역 토큰이
부족하다는 신호이지 새 스코프가 필요하다는 신호가 아니다.

---

## 7. 우선순위 (제안)

1. **폰트 스택에 한글 추가** — 비용 한 줄, 효과 전 화면. 지금 자간 주는 곳마다 글자가 벌어진다.
2. **표류 색 4개 정리**(`#22c55e` `#4ade80` `#888` `#666`) — 눈에 보이는 불일치의 실체.
3. **타이포·라운드 스케일 토큰 신설** — 새 코드가 참조할 기준이 생긴다. 기존 코드 일괄 치환은 하지 않는다.
4. **그림자 3단계** — 카드 깊이가 들쭉날쭉한 것 해소.
5. 토큰 우회(2,449곳)는 **화면별로 손댈 때 그 파일만** 정리. 일괄 치환은 위험 대비 이득이 낮다.

> ⚠ 회귀 주의: 이 저장소는 **등락색·신호색 분리**(§1-1)와 **관제실 스코프**(§6)가 의도된 설계다.
> "중복 제거" 로 합치면 한국 관례 표시가 깨지거나 전역 테마가 무너진다.
