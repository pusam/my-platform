name: Deploy to Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 소스 코드 가져오기
      - name: Checkout source code
        uses: actions/checkout@v4

      # =========================================================
      # [추가됨] 2. 프론트엔드 빌드 (Node.js)
      # 목표: 리액트/뷰를 빌드해서 -> 스프링 부트의 static 폴더로 복사
      # =========================================================
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Frontend and Copy to Backend
        run: |
          echo "🚧 프론트엔드 빌드 시작..."
          cd frontend
          
          # 의존성 설치 및 빌드
          npm install
          npm run build
          
          # 백엔드의 정적 리소스 폴더로 이동 (폴더 없으면 생성)
          # 주의: backend 모듈이 따로 있는 구조라고 가정했습니다. 
          # 만약 루트가 바로 백엔드라면 '../src/main/resources/static' 으로 고치세요.
          echo "🚚 빌드 파일 복사 중..."
          mkdir -p ../backend/src/main/resources/static
          
          # 기존 파일 삭제 (깨끗하게)
          rm -rf ../backend/src/main/resources/static/*
          
          # 빌드 결과물 복사 (Vite는 dist, CRA는 build 확인 필요!)
          # 만약 'cp: cannot stat 'dist/*': No such file' 에러 나면 dist를 build로 바꾸세요.
          cp -r dist/* ../backend/src/main/resources/static/
          
          echo "✅ 프론트엔드 통합 완료!"

      # =========================================================
      # 3. 백엔드 빌드 (Java)
      # 이제 static 폴더에 프론트엔드 파일이 들어있는 상태로 JAR가 만들어짐
      # =========================================================
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        run: |
          chmod +x gradlew
          ./gradlew bootJar -x test

      # =========================================================
      # 4. 서버로 전송 (SCP)
      # =========================================================
      - name: Copy files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key: ${{ secrets.KEY }}
          source: "backend/build/libs/*.jar,docker-compose.yml,Dockerfile,nginx,setup-database.sql,insert-default-data.sql"
          target: "/home/dev/my-platform"
          strip_components: 0

      # =========================================================
      # 5. 서버 재시작 (SSH)
      # =========================================================
      - name: Restart Docker Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key: ${{ secrets.KEY }}
          script: |
            cd /home/dev/my-platform
            
            # JAR 파일 이름 통일 (버전 떼기)
            mv backend/build/libs/*.jar app.jar 2>/dev/null || true
            
            # 전송된 임시 폴더들 정리
            rm -rf backend
            
            # 도커 재시작 (이미지 새로 굽기)
            docker compose down
            docker compose up -d --build
            
            # 잘 떴는지 확인
            docker compose ps
            
            # 안 쓰는 이미지 삭제 (용량 확보)
            docker image prune -f