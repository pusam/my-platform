-- ============================================================
-- V1: Baseline Migration
-- 기존 테이블이 있는 경우 이 파일은 건너뜀 (baseline-on-migrate)
-- 새로운 DB의 경우 JPA ddl-auto=update가 테이블 생성
-- ============================================================

-- Flyway가 관리할 마이그레이션 시작점
-- 이 파일은 기존 DB에서는 실행되지 않음 (baseline-version: 0)
SELECT 1;
